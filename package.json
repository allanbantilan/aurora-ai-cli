{
  "name": "aurora",
  "version": "0.1.0",
  "description": "CLI coding agent with tool use, provider fallback, and Laravel-aware workflows",
  "type": "module",
  "bin": {
    "aurora": "bin/aurora.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "eval": "node scripts/eval.js",
    "eval:live-matrix": "node scripts/eval.js --live-matrix"
  },
  "dependencies": {
    "openai": "^6.42.0",
    "picomatch": "^4.0.4"
  }
}
